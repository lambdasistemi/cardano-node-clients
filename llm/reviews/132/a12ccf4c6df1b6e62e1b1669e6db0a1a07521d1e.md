# Ready for review: a12ccf4c6df1b6e62e1b1669e6db0a1a07521d1e

state: WaitingForCodeReview
sha: a12ccf4c6df1b6e62e1b1669e6db0a1a07521d1e
slice: T004-T010 / Slice A certificate instructions
red: `nix develop --quiet -c cabal test cardano-node-clients:unit-tests -O0 --test-show-details=direct --test-option=--match --test-option='/TxBuild/certify/'` failed before implementation because `certify`, `PubKeyCert`, and `ScriptCert` were not in scope.
green: Added `CertWitness`, `certify`, certificate accumulation in `TxState`, `certsTxBodyL` assembly, and `ConwayCertifying` redeemer collection at final body-field indices.
gate: `nix develop --quiet -c ./llm/reviews/132/gate.sh` passed before handoff: build, unit-tests, tx-build-tests, fourmolu check, hlint, and cabal-fmt check.

## Notes

Slice A is intentionally limited to certificate body entries and certificate redeemers. Treasury withdrawals and governance proposal procedures remain in later slices.

## Solo Review Verdict

Approved for Slice A. The commit is a single vertical, bisect-safe slice with RED/GREEN evidence recorded above, and the public API/docs/task updates travel with the implementation.
