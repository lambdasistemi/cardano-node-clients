# Ready for review: 2b79dc9d755c2eb22f97f563cabe202659e9769f

state: WaitingForCodeReview
sha: 2b79dc9d755c2eb22f97f563cabe202659e9769f
slice: CI stabilization / issue #97 repro predicate
red: `nix develop --quiet -c just ci` failed in `e2e-tests` because the issue #97 relay-restart repro returned `Left (IOException Network.Socket.ByteString.sendMany: resource vanished (Broken pipe) "sendAll errored")`, which the predicate did not classify as the same peer-close failure mode.
green: Broadened the issue #97 repro predicate to accept socket-level `Broken pipe` / `resource vanished` text for the same upstream relay-close behavior.
gate: `nix develop --quiet -c cabal test cardano-node-clients:e2e-tests -O0 --test-show-details=direct --test-option=--match --test-option='/issue #97 — BearerClosed escape from runChainSyncN2C/runChainSyncN2C terminates with a BearerClosed-like exception when the upstream relay restarts/'` passed with 1 example and 0 failures.

## Notes

This is intentionally a narrow test-only stabilization commit. It does not change production code or the Slice A certificate implementation.

## Solo Review Verdict

Approved. The failure was in a live-boundary e2e predicate that was too specific about the rendered exception text for the same relay-close condition.
