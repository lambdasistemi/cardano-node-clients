# Ready for review: e744e782251d94350f8595e889a84ed26bd9e24b

state: WaitingForCodeReview
sha: e744e782251d94350f8595e889a84ed26bd9e24b
slice: Review finding P1 / duplicate Conway certificate values
red: `nix develop --quiet -c cabal test cardano-node-clients:unit-tests -O0 --test-show-details=direct --test-option=--match --test-option='/TxBuild/certify/indexes duplicate certificate occurrences independently/'` failed with duplicate cert calls returning `(0,0)` rather than the hypothesized `(0,1)`.
green: Hoogle confirmed `certsTxBodyL :: ... StrictSeq ...`, while `ConwayTxBody` internally stores certs as `OSet (TxCert ConwayEra)`. The final regression now asserts the actual body contract: duplicate equal certificates coalesce to one final body index, and `collectCertRedeemers` emits one deterministic redeemer for that index instead of relying on accidental `Map.fromList` collapse.
gate: `nix develop --quiet -c cabal test cardano-node-clients:unit-tests -O0 --test-show-details=direct --test-option=--match --test-option='/TxBuild/certify/'` passed with 3 examples and 0 failures. `nix develop --quiet -c ./llm/reviews/132/gate.sh` passed with unit 225/0, tx-build 34/0, fourmolu, hlint, and cabal-fmt.

## Notes

The external finding's "second body entry" premise does not hold for Conway: equal certs coalesce in the final body. This fix makes the coalescing explicit and deterministic.

## Solo Review Verdict

Approved. The implementation now matches final body semantics and documents the ordered-set behavior in the Spec Kit artifacts.
